name: binary_map_file
description: "Performant key-value serialization file based in pure Dart, secured support"
version: 0.0.1
homepage: https://github.com/annotium/binary_map_file

environment:
  sdk: '^3.3.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  crypto: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  mocktail: ^1.0.3

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
